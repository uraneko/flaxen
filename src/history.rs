trait History {
    // filters the history entries by the already written input pattern
    // excluding entries that dont contain the latter
    fn pat_fltr() {}

    // loads history entries from cached data wether in file or db
    fn load() {}
}
